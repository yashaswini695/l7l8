#include <stdio.h>

int main() {
    int num1, num2, sum;

    // Input two numbers
    printf("Enter two numbers: ");
    scanf("%d %d", &num1, &num2);

    // Add the numbers
    sum = num1 + num2;

    // Display the result
    printf("Sum = %d\n", sum);

    return 0;
}
